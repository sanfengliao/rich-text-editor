{
  "name": "rich-text-editor",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "c8": "^7.11.0",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "vitest": "^0.7.7"
  }
}
